import sparkline from 'frontend/utils/sparkline';
import { module, test } from 'qunit';

module('Unit | Utility | sparkline');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sparkline();
  assert.ok(result);
});
